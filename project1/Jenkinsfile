
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('project1') {
                    sh('./project1/p1.sh')
                }
            }
        }
    }
}