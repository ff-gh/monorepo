
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('project1') {
                    sh('pwd; ls ; bash p1.sh')
                }
            }
        }
    }
}