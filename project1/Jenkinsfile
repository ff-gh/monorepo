
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('project1') {
                    script {
                        sh('p1.sh')
                    }
                }
            }
        }
    }
}