
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('project1') {
                    sh('pwd; ls ; project1/p1.sh')
                }
            }
        }
    }
}