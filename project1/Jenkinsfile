
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                dir('project1') {
                    sh('./p1.sh')
                }
            }
        }
    }
}